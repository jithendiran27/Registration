import { StepsDirective } from './steps.directive';

describe('StepsDirective', () => {
  it('should create an instance', () => {
    const directive = new StepsDirective();
    expect(directive).toBeTruthy();
  });
});
